import type { Client } from "@hey-api/client-fetch";
import { SandboxClient, SandboxStartData, CreateSandboxOpts, VMTier, SandboxListOpts, SandboxInfo } from "./sandbox-client";
export { SandboxClient, SandboxStartData, CreateSandboxOpts, VMTier, SandboxListOpts, SandboxInfo, };
export * from "./sandbox";
export interface ClientOpts {
    baseUrl?: string;
    /**
     * Custom fetch implementation
     *
     * @default fetch
     */
    fetch?: typeof fetch;
    /**
     * Additional headers to send with each request
     */
    headers?: Record<string, string>;
}
export type SandboxPrivacy = "public" | "unlisted" | "private";
export declare class CodeSandbox {
    private readonly opts;
    private baseUrl;
    private apiToken;
    readonly apiClient: Client;
    readonly sandbox: SandboxClient;
    constructor(apiToken?: string, opts?: ClientOpts);
}
